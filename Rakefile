# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/github_hook.rb'

Hoe.new('GithubHook', GithubHook::VERSION) do |p|
  # p.rubyforge_name = 'GithubHookx' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
